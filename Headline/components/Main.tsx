import {NativeBaseProvider} from "native-base";

interface Article {
    interest: string
    title: string
    link: string
}

const data = [

]

export default function Main() {
    return (
        <NativeBaseProvider>

        </NativeBaseProvider>
    )
}
